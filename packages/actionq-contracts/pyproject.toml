[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "actionq-contracts"
version = "0.1.0"
requires-python = ">=3.11"
description = "Portable, dependency-light ActionQ execution contracts"

[tool.setuptools.packages.find]
include = ["actionq_contracts*"]
